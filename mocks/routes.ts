enum Routes {
  Login = '**/login',
  Users = '**/api/users',
  Boards = '**/api/boards',
  Board = '**/api/boards/*',
  Lists = '**/api/lists/*',
  Tasks = '**/api/tasks/*',
}

export default Routes;
